[project]
name = "golgi"
version = "0.1.0"
description = "Conversation viewer and memory manager"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "openai>=1.0.0",
    "pytest>=7.0.0",
]

[tool.hatch.build.targets.wheel]
packages = []
